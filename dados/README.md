# dados/

Scripts SQL (migrations do Supabase), exports/backups do ERP e qualquer coisa relacionada aos dados do catálogo — não ao código do site em si. Serve como registro do que foi rodado no banco, pra dar pra reconstruir do zero se precisar.
